version: "3.9"

services:
  inference-api:
    build: .
    container_name: inference-api
    ports:
      - "8000:8000"
    environment:
      MODEL_OUT_DIR: /app/outputs_candidate_generation
    restart: unless-stopped
